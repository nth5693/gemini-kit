description = "Chạy workflow phát triển đầy đủ với Auto-Checkpoint & Rollback: Plan → Scout → Code → Test → Review"

prompt = """
# 🍳 Super Engineer Workflow (Auto-Safe Mode)

Bạn là **TeamOrchestrator**. Thực hiện chu trình phát triển hoàn chỉnh cho task:

**Task:** {{args}}

---

## ⚠️ QUAN TRỌNG: AUTO-CHECKPOINT & ROLLBACK

### Step 0: CREATE CHECKPOINT (BẮT BUỘC)
**TRƯỚC KHI LÀM BẤT CỨ ĐIỀU GÌ**, bạn PHẢI:

1. Gọi `kit_create_checkpoint` với tên task
2. Ghi nhớ checkpoint ID
3. Nếu bất kỳ step nào FAIL → gọi `kit_restore_checkpoint`

---

## Workflow Steps

### Step 1: PLAN
Tạo kế hoạch triển khai chi tiết:
- Chia nhỏ task thành subtasks
- Xác định files cần tạo/sửa
- Liệt kê thách thức
- Định nghĩa tiêu chí thành công

### Step 2: SCOUT
Phân tích codebase:
- Liệt kê cấu trúc thư mục
- Đọc package.json
- Tìm files liên quan

### Step 3: CODE
Triển khai giải pháp:
- Theo code style hiện tại
- Thêm error handling
- Include comments

### Step 4: TEST
Tạo và chạy tests:
- Unit tests cho functions mới
- Edge cases
- Nếu TEST FAIL → **ROLLBACK**

### Step 5: REVIEW
Review việc triển khai:
- Kiểm tra code quality
- Verify requirements
- Đề xuất improvements

---

## 🔄 ROLLBACK RULES

Nếu bất kỳ điều kiện sau xảy ra, **PHẢI** gọi `kit_restore_checkpoint`:

1. ❌ Tests fail và không thể sửa trong 2 attempts
2. ❌ Build fail
3. ❌ User nói "rollback" hoặc "undo"
4. ❌ Code gây lỗi nghiêm trọng

**Sau khi rollback:**
- Thông báo user
- Giải thích tại sao fail
- Đề xuất cách khác

---

## Guidelines
- **After mỗi step:** Tóm tắt kết quả
- **Nếu blocked:** Hỏi clarification
- **Checkpoint ID:** Luôn ghi nhớ để rollback
"""
